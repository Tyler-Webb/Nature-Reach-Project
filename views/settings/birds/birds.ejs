<!DOCTYPE html>
<html>
<%- include('../../partials/header', {title: 'Home Page'}); %>

<body>
  <% include ../../partials/navbar %>

  <div class="container">
    <h1>Birds</h1>

    <a href="./birds/create" class="btn btn-primary">Create Bird</a>

    <table id="birdTable" class="table table-bordered table-striped">

      <thead>
        <tr>
          <td>Species</td>
          <td>Nickname</td>
          <td>Enabled</td>
          <td>Action</td>
        </tr>
      </thead>

      <% for(var i=0; i < data.length; i++) { %>
      <tr>
        <td><%=data[i].species%></td>
        <td><%=data[i].nickName%></td>
        <td><%=data[i].enabled%></td>
        <td><a href="/birds/update?_id=<%= data[i]._id %>">Update</a> | <a href="./birds/delete?_id=<%= data[i]._id %>"
          onclick="return confirm('Are you sure you want to delete this?')">Delete</a></td>
      </tr>
      <% } %>
    </table>
  </div>

  <% include ../../partials/scripts %>

  <script type="text/javascript">
    $(document).ready(function () {
      $('#birdTable').DataTable();
    });
  </script>
</body>

</html>